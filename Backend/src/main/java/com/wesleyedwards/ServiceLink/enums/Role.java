package com.wesleyedwards.ServiceLink.enums;

public enum Role {
    ADMIN,  // full access, can manage users
    AGENT,  // can be assigned tickets, manage tickets
    USER    // can create tickets, add comments
}
